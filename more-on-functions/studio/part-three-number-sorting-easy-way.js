//Sample arrays for testing:
let nums1 = [5, 10, 2, 42];
let nums2 = [-2, 0, -10, -44, 5, 3, 0, 3];
let nums3 = [200, 5, 4, 10, 8, 5, -3.3, 4.4, 0];


//Sort each array in ascending order.
function sortingStuffUp(nums) {
    let storingArray = [];
    for (let i = 0; i <nums.length;ci++) {
        minimum(nums[i])
    storingArray.push(smally)
}
return storingArray
    }
//Sort each array in decending order.
function sortingStuffDown(nums) {
    let storingArrayA = [];
    for (let i = 0; i <nums.length;ci++) {
        minimum(nums[i])
    storingArrayA.unshift(smally) 
    }
    return storingArrayA
}
console.log(sortingStuffUp(nums1));
console.log(sortingStuffDown(nums1));

